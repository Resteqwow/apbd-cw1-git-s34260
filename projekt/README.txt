wykonanie merge po zmieniu rzeczy w mainie zamiast wykonania merge fast_forward wykonalo merge z strategia ort 
(nie musialem nic zmieniac zeby merge sie udal) 
merge typu fast-forward wykonuje sie gdy nie ma splitowania branchow 
merge a rebase  
merge nie zmienia historii widac rozgalenzienia rebase natomiast zmienia historie na liniowa 
konflikt w moim repozytorium zostal rozwiazany poprzez usuniecie funkcji z konfliktujacego branchu 